import React from "react";
import { LoginForm } from "_components";

export const metadata = {
	title: "Login",
};

const Login = () => {
	return (
		<>
			<LoginForm />
		</>
	);
};

export default Login;
